<h2> <%= @task.name %> </h2>

<%= form_for([@list, @task]) do |task| %>
  <%= form.text_field :name, placeholder: :name %>
  <%= form.text_field :description, placeholder: :description %>
  <%= form.submit "Submit" %>
<% end %>